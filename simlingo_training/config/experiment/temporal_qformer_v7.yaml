# @package _global_
defaults:
  - /data_module: carla_bucket_v12_dreamer
  - /model/vision_model: vlm
  - /data_module/base_dataset: dataset

model:
  lr: 3e-5
  freeze_language_model: false
  freeze_adaptors: false
  freeze_wp_encoder: true
  predict_route_as_wps: True
  speed_wps_mode: 2d
  language_model:
    variant: 'OpenGVLab/InternVL2-1B'
    lora: True
    lora_alpha: 64
    lora_r: 32
    lora_dropout: 0.1
  vision_model:
    variant: 'OpenGVLab/InternVL2-1B'
    freeze: true
  temporal_model:
    enabled: true
    num_queries: 32
    num_layers: 2
    num_heads: 8
    dropout: 0.1
    gate_enabled: false
    gate_init: 0.0
    use_current_delta: true
    include_past_features: true
    include_absolute_delta: true
    aux_loss_weight: 0.5
    dynamic_sample_weight: 3.0

data_module:
  batch_size: 6
  num_workers: 8
  base_dataset:
    data_path: database/simlingo
    bucket_path: database/bucketsv2_simlingo
    pred_len: 11
    cut_bottom_quarter: True
    use_commentary: True
    use_qa: True
    qa_augmentation: True
    img_shift_augmentation: True
    img_shift_augmentation_prob: 0.5
    hist_len: 5
    history_stride: 1
    route_as: target_point_command
    use_lmdrive_commands: True
    use_old_towns: True
    use_town13: True
    use_safety_flag: True
    use_motion_descriptions: True
    use_motion_prompt: True

max_epochs: 15
val_every_n_epochs: 2

gpus: 8
seed: 9876
name: temporal_qformer_v7
resume: False
reset_llm_lora_from_checkpoint: true

hydra:
  run:
    dir: outputs/${wandb_name}_${name}
